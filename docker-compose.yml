services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: spanpay
      POSTGRES_USER: spanpay
      POSTGRES_PASSWORD: spanpay
    ports:
      - "6942:5432"
    volumes:
      - ./db/data:/var/lib/postgresql/data
